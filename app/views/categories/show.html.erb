<%= link_to "&larr; Back".html_safe, :back, :class => 'dropbtn' %>

<h2><%= @category.category_text %></h2>

<% @posts.each do |post| %>
<%= post.title %>
<img src= "<%= post.photo_url %>" />
<% end %>
