<%= simple_form_for ([@post, @guess]) do |f| %>

  <%= f.input :guess_text, :input_html => { :class => 'comment-form' }, label: 'Comment', error: 'Enter comment!' %>

  <%= f.button :submit, 'Make comment!' %>

<% end %>
