<h1>title goes here</h1>
<%= link_to 'Add your location!', new_place_path %>

<% @places.each do |place| %>
  <p><%= link_to place.city, place_posts_path(place)  %>, <%= place.state %></p>
  <p><%= place.country %></p>
  <img src="<%=place.place_photo_url%>"/>
<% end %>
